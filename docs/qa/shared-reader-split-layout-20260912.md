# 共享阅读器分栏双页验收

状态：下列命名路径有限接受，共享源码检查点 `3dba868`；默认替换仍为 OPEN。仅可选实验入口，不读写迁移原进度，不改变生产阅读器入口。

## 范围与标准

沿用现有双页父级：一个分页容器、一个缩放所有者、两个等宽栏位，图片各自等比适配。拼接双页仍为等高比例布局；末尾只有一张图时仍用单页适配。分栏不改变原页配对，也不等于宽图裁成两片。

接受条件分开记录：

- 显示：普通、长条、横宽图片保持比例；两个栏位及末尾单页正确。
- 缩放：实际缩放、实际拖动、左右外缘分别可达；仅命令成功或页码不变不算通过。
- 状态：横竖屏、分栏与拼接切换保留命名源页；退出恢复宿主窗口及测试设置。
- 异常：加载失败、重试、裁边及转场组合未执行的分支保留 OPEN，不由正常图片覆盖。
- 三个宿主分别验证；103受控测试漫画不代表所有真实来源。

## 拦截的缺陷

旧候选对两个栏位整体缩放，却以两张图片宽度之和限制横向拖动，遗漏中间空隙。197长条样本在2倍时仍被限制为x=0。

旧运行 `08-horizontal-native` 的诊断脚本报告1通过，但只检查了执行流程与页码；PID9832的实际变换及前后图片不支持水平移动。因此其水平可达性判为失败，不能引用测试汇总放行。

修复以每张图片所在栏位的实际左右边界求并集，并保留非对称中心。原有单页、拼接布局默认中心仍为0。数学回归包含窄长图双页、混合比例、单侧溢出以及默认计算等价性。

## 固定候选与证据

| 宿主 | 主包标识 | 当前证据 |
| --- | --- | --- |
| NextN | 构建49045；04:33:44；46976721字节 | 197 `15-visible-reach` 配对native31363，1通过/25.030秒；`19-ready-full` 配对native8893，1通过/61.083秒；命名端点、变换和退出原图均已复核 |
| NextE | 构建8504；04:35:17；62639693字节 | 197 `22-nexte-full` 配对native67674，1通过/51.167秒；水平拖动、实际双指、旋转、拼接返回及末页单图原图已复核；双向端点仍OPEN |
| Koma | 构建28633；04:39:20；14466804字节 | 103混合比例适配、左右可达、横竖屏保留2/3、返回拼接、切回分栏后末页3/3及退出恢复已复核；真实双指仍未验证 |

固定候选真实方法测试278/278通过。此数量不是设备通过率。

NextN `11-fixed-reach`：PID17255，scale2、selection8不变，横向x依次为0、-60.38、65.18vp。截图支持两侧外缘回到屏幕。脚本0通过/1错误25.231秒仍保留，不篡改为通过；更新后的脚本要求同一可见稳定图片发生正反方向位移，完整原图另行判断端点。

NextN后续 `15-visible-reach` 在PID20249重复得到相同变换边界，同一左图反向位移408px，原始脚本通过且主控复核两张端点原图。`16-visible-full` 则在旋转往返后立即查询菜单项得到null；异常原图中该菜单项实际存在，清理点击又被菜单消费，末尾退出断言失败。保留失败，不以菜单截图替代脚本通过；下一步修正测试的语义等待及菜单关闭顺序，产品不因该测试错误改动。

修正后的 `19-ready-full`：PID24766，2倍缩放后实际纵向y从-19.08到-418.46vp，实际双指scale2.454后双击回fit1；横屏与竖屏往返仍为源页1/14，返回拼接图边界与初始相同。末页14/14在拼接及分栏均为相同单图边界，退出到普通宿主，原orientation12恢复。主控读原始测试/变换日志并检查全部10张命名完整截图，旧exception文件不纳入成功证据。

Koma103：主控复核03/04/05/06/08/09/10/11/12完整原图、对应布局及同PID30940变换。04→08适配复位的正文像素一致；03→09两张图边界相同。12退出实际书架，MUSIC9、OverrideTimeout10000恢复；库及会话SHA256与01基线一致。右边缘可见已观察，但没有重复最终x值，不宣称右侧饱和测量。真实双指缩放仍为OPEN。

NextE `22-nexte-full`：PID27249，2倍实际水平拖动x0→-193.85vp，双指scale2.482后回fit1；横竖屏及返回拼接保留源页1/46，末页46/46两种呈现都是同一单图边界。主控复核全部10张命名原图和原始日志。`21-nexte-reach` 在第一段拖动后查询已完全出屏图片返回null，0通过/1错误25.908秒，未执行反向；保留其错误，不能外推E双向端点通过。最终N/E测试统一可见稳定id交集、独立菜单锚点清理并保留原始异常，编译N69094/E92697通过，未作为新设备通过版本冒充。23退出恢复原超时10000和方向12，197租约释放。

本地证据目录：

- `.hermes-artifacts/20260912-spread-layout-197/`
- `.hermes-artifacts/20260912-split-ratio-koma-103/`：旧候选与独立样本导入记录。
- `.hermes-artifacts/20260912-split-center-koma-103/`：固定候选。

## 未关闭项

NextE双向端点仍OPEN。Koma真实双指缩放未取得设备证据；双击不能替代。裁边、失败重试、转场与分栏组合也不能由当前正常图路径推定通过。

先完成该切片，再继续只读初始模式衔接。初始设置读取失败、关闭后迟到结果和缩略图转场未揭示阶段必须独立测试，不能先默认打开再异步切换。
