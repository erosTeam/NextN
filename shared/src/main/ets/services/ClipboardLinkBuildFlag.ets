/** Full source builds keep clipboard detection enabled; public CI disables it before compiling. */
export class ClipboardLinkBuildFlag {
  static readonly CLIPBOARD_LINK_BUILD_ENABLED: boolean = true
}
